

## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url = jdbc:mysql://localhost:3306/exchange-app?useSSL=false
spring.datasource.username = root
spring.datasource.password = root24


## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

## This is important
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = validate
spring.jpa.show-sql=true


exchange.rate.baseurl =https://api.frankfurter.app
exchange.rate.from.currency=USD
exchange.rate.to.currency=EUR,GBP,JPY,CZK